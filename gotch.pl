check(typo).
check(server).
check(database).
check(password).
check(port).
check(diff).
check(version).

check(sendgrid).
check(dns).
check(rfc).
check(uppercase).

check(offbyone).
check(month).
check(timezone).
check(utc).
check(jst).

check(whitespace).
check(tab).
check(utf8).
check(cp932).
check(bom).

check(transpose).

check(annotation).
check(test).

snipet(__FILE__).
snipet(__LINE__).
snipet(__FUNCTION__).
